import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    container: {
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    authorView:{
        marginTop:10,
        borderColor:"black",
        borderWidth:1,
        borderRadius:5,
        width:350
     },
     buttonView:{
        backgroundColor:"red",
        marginTop:20,
        borderRadius:5,
        width:80,
        height:40,
        alignItems:"center",
        justifyContent:"center"
    }
  });
  